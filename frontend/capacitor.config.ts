import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.reviewassistant.app',
  appName: 'Review Assistant',
  webDir: '.output/public',
}

export default config
